version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: src/Dockerfile
    ports:
      # Bind to localhost only for security - no external access
      - "127.0.0.1:5000:5000"
    restart: unless-stopped
